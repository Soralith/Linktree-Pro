@extends('layouts.app')

@section('content')
<div class="hero-banner">
    <div class="container">
        <h1 class="hero-title">Portfolio & Projects</h1>
        <p class="hero-subtitle">Explore my collection of work across different categories</p>
    </div>
</div>

<div class="container py-5">
    @foreach($categories as $category)
        @if($category->products->count() > 0)
            <div class="category-section">
                <div class="category-header">
                    <div class="d-flex align-items-center gap-3">
                        @if($category->icon)
                            <img src="{{ asset('storage/' . $category->icon) }}" alt="{{ $category->name }}" class="category-icon">
                        @endif
                        <div>
                            <h2>{{ $category->name }}</h2>
                            <p class="category-count">{{ $category->products->count() }} {{ $category->products->count() === 1 ? 'project' : 'projects' }}</p>
                        </div>
                    </div>
                </div>

                <div class="products-grid">
                    @foreach($category->products as $product)
                        <article class="product-card">
                            <a href="{{ route('product.show', $product->slug) }}" class="product-link">
                                <div class="product-thumbnail">
                                    @if($product->thumbnail)
                                        <img src="{{ asset('storage/' . $product->thumbnail) }}" alt="{{ $product->title }}">
                                        <div class="product-overlay">
                                            <span class="view-project">View Project <i class="bi bi-arrow-right"></i></span>
                                        </div>
                                    @else
                                        <div class="product-placeholder">
                                            <i class="bi bi-image"></i>
                                        </div>
                                    @endif
                                </div>
                                <div class="product-info">
                                    <h3>{{ $product->title }}</h3>
                                    <p>{{ Str::limit($product->description, 90) }}</p>
                                    <div class="product-meta">
                                        <span class="category-badge">{{ $category->name }}</span>
                                        @if($product->demo_link)
                                            <span class="has-demo"><i class="bi bi-globe"></i> Live Demo</span>
                                        @endif
                                    </div>
                                </div>
                            </a>
                        </article>
                    @endforeach
                </div>
            </div>
        @endif
    @endforeach

    @if($categories->sum(fn($c) => $c->products->count()) === 0)
        <div class="empty-state">
            <i class="bi bi-inbox"></i>
            <h3>No Projects Yet</h3>
            <p>Check back soon for exciting new projects!</p>
        </div>
    @endif
</div>

<style>
.hero-banner {
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
    color: white;
    padding: 5rem 0;
    text-align: center;
    margin-bottom: 4rem;
    position: relative;
    overflow: hidden;
}
.hero-banner::before {
    content: '';
    position: absolute;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(233,69,96,0.15) 0%, transparent 70%);
    top: -250px;
    right: -250px;
}
.hero-title {
    font-size: 3.5rem;
    font-weight: 800;
    margin-bottom: 1rem;
    letter-spacing: -0.02em;
}
.hero-subtitle {
    font-size: 1.35rem;
    opacity: 0.9;
    font-weight: 300;
}
.category-section {
    margin-bottom: 5rem;
}
.category-header {
    margin-bottom: 2.5rem;
}
.category-icon {
    width: 56px;
    height: 56px;
    object-fit: contain;
    padding: 8px;
    background: var(--bg-light);
    border-radius: 12px;
}
.category-header h2 {
    font-size: 2rem;
    font-weight: 700;
    margin: 0;
    color: var(--primary-color);
}
.category-count {
    color: var(--text-light);
    margin: 0;
    font-size: 0.95rem;
}
.products-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 2.5rem;
}
.product-card {
    background: white;
    border-radius: 20px;
    overflow: hidden;
    border: 1px solid var(--border-color);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}
.product-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 40px rgba(0,0,0,0.1);
    border-color: var(--accent-color);
}
.product-link {
    text-decoration: none;
    color: inherit;
    display: block;
}
.product-thumbnail {
    position: relative;
    width: 100%;
    height: 240px;
    overflow: hidden;
    background: var(--bg-light);
}
.product-thumbnail img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}
.product-card:hover .product-thumbnail img {
    transform: scale(1.08);
}
.product-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(26,26,46,0.9), transparent);
    display: flex;
    align-items: flex-end;
    justify-content: center;
    padding: 1.5rem;
    opacity: 0;
    transition: opacity 0.3s;
}
.product-card:hover .product-overlay {
    opacity: 1;
}
.view-project {
    color: white;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 1rem;
}
.product-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 4rem;
    color: var(--text-light);
    background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
}
.product-info {
    padding: 1.75rem;
}
.product-info h3 {
    font-size: 1.25rem;
    font-weight: 700;
    line-height: 1.4;
    margin-bottom: 0.75rem;
    color: var(--primary-color);
}
.product-info p {
    font-size: 0.95rem;
    color: var(--text-light);
    margin-bottom: 1rem;
    line-height: 1.6;
}
.product-meta {
    display: flex;
    gap: 0.75rem;
    align-items: center;
    flex-wrap: wrap;
}
.category-badge {
    background: var(--bg-light);
    color: var(--text-dark);
    padding: 0.4rem 0.8rem;
    border-radius: 8px;
    font-size: 0.8rem;
    font-weight: 600;
}
.has-demo {
    color: var(--accent-color);
    font-size: 0.85rem;
    font-weight: 500;
    display: flex;
    align-items: center;
    gap: 0.3rem;
}
.empty-state {
    text-align: center;
    padding: 6rem 2rem;
}
.empty-state i {
    font-size: 5rem;
    color: var(--text-light);
    margin-bottom: 1.5rem;
    opacity: 0.5;
}
.empty-state h3 {
    font-size: 1.75rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    color: var(--primary-color);
}
.empty-state p {
    color: var(--text-light);
    font-size: 1.05rem;
}
@media (max-width: 768px) {
    .hero-title {
        font-size: 2.5rem;
    }
    .hero-subtitle {
        font-size: 1.1rem;
    }
    .products-grid {
        grid-template-columns: 1fr;
        gap: 1.5rem;
    }
}
</style>
@endsection
